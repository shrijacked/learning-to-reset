# Automated replication (CPU or GPU). Install from repo root after creating a venv:
#   pip install -r requirements.txt && pip install -e .
#
torch>=2.1.0,<3
transformers>=4.38.0
datasets>=2.16.0
accelerate>=0.27.0
tqdm>=4.66.0
numpy>=1.24.0
huggingface-hub>=0.20.0
safetensors>=0.4.0
sentencepiece>=0.1.99
protobuf>=4.21.0
regex>=2023.0.0
