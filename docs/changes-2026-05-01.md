# Changes - 2026-05-01

## Issue: placeholder-step1-hard-split

- Hypothesis:
  Making `hard-train`, `hard-mine`, and `hard-holdout` explicit will remove the current split-role ambiguity and make failure mining reliable without contaminating final hard evaluation.
- Change made:
  Exported explicit hard subsets during paper artifact preparation:
  - `countdown-train-hard.jsonl`
  - `countdown-mine-hard.jsonl`
  - `countdown-test-hard.jsonl`

  Updated the replication script so raw failure mining runs on `countdown-mine-hard.jsonl` and final evaluation stays on `countdown-test-hard.jsonl`.
- Result (metrics):
  Local verification passed:
  - `PYTHONPATH=src python3 -m unittest tests.test_paper_dataset_prep tests.test_prepare_paper_cli`
  - `LTR_REPLICATE_DRY_RUN=1 PYTHONPATH=src python3 -m unittest tests.test_replicate_paper_dry_run`

  Both suites passed with no failures.
- Conclusion:
  This establishes the clean experimental foundation needed before any arithmetic-correctness interventions. It does not directly improve correctness by itself; it fixes the data-role boundary so later correctness gains can be trusted.

## Issue: placeholder-step2-true-raw-baseline

- Hypothesis:
  A true raw baseline must remove clean instructions from the prompt itself; otherwise raw generation is partly measuring a prompt-policy mismatch instead of pure arithmetic ability.
- Change made:
  Exported raw prepared Countdown variants with `allow_clean=False`, including:
  - `countdown-test-raw.jsonl`
  - `countdown-test-hard-raw.jsonl`
  - `countdown-mine-hard-raw.jsonl`

  Updated the replication script so Step 4 raw failure mining uses `countdown-mine-hard-raw.jsonl` instead of a clean-enabled prompt file.
- Result (metrics):
  Local verification passed:
  - `PYTHONPATH=src python3 -m unittest tests.test_paper_dataset_prep tests.test_prepare_paper_cli`
  - `LTR_REPLICATE_DRY_RUN=1 PYTHONPATH=src python3 -m unittest tests.test_replicate_paper_dry_run`

  Both suites passed with no failures.
- Conclusion:
  This gives us a true arithmetic-only raw baseline by removing `<clean>` from the prompt itself. It improves measurement quality and makes raw-vs-reset-aware comparisons interpretable; it does not itself raise arithmetic correctness.

## Issue: placeholder-step3-sft-data-mix-audit

- Hypothesis:
  The SFT mix is too reset-heavy, especially because bootstrap-generated clean-only negatives add clean supervision without directly improving arithmetic correctness. Auditing and reducing that skew should improve the quality of later correctness-focused training.
- Change made:
  Added SFT mix auditing by:
  - source
  - stage
  - recovery style
  - bootstrap kind
  - clean usage

  Wrote `sft-mix-summary.json` during paper artifact preparation and added a first reduction lever: excluding bootstrap-generated `think_only_negative` examples from the paper-style SFT mix.
- Result (metrics):
  Local verification passed:
  - `PYTHONPATH=src python3 -m unittest tests.test_pipeline tests.test_paper_dataset_prep tests.test_prepare_paper_cli tests.test_data_pipeline`
  - `LTR_REPLICATE_DRY_RUN=1 PYTHONPATH=src python3 -m unittest tests.test_replicate_paper_dry_run`

  Both suites passed with no failures.
- Conclusion:
  The repo now produces an explicit SFT mix audit and excludes bootstrap clean-only negatives in the paper-style prep path. This is a conservative first skew-reduction pass aimed at improving arithmetic-focused supervision quality before more RL.
